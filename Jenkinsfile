node {
    stage('Example') {
        if (env.BRANCH_NAME == 'develop') {
            echo 'I only execute on the develop branch'
        } else {
            echo 'I execute elsewhere'
        }
    }
}